{
  "name": "sokech-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-resume": "node scripts/generate-resume.js"
  },
  "dependencies": {
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "puppeteer": "^23.0.0",
    "vite": "^5.4.0"
  }
}
