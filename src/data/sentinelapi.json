{
  "meta": {
    "slug": "sentinel-api",
    "status": "in-progress",
    "completed_month": "TBD",
    "duration_weeks": null,
    "team_size": "Solo",
    "featured": false
  },

  "header": {
    "title": "SentinelAPI",
    "subtitle": "Intelligent API gateway with ML-powered anomaly detection and automated threat response",
    "live_url": null,
    "github_url": null
  },

  "summary": {
    "plain_english": "An API gateway layer that sits in front of any backend service and learns normal traffic patterns using machine learning. When requests deviate from baseline — unusual volumes, geographic anomalies, credential-stuffing patterns — the system automatically rate-limits, blocks, or escalates the threat without human intervention."
  },

  "motivation": {
    "quote": "Most API security tooling is rule-based and brittle — you write a rule, attackers adapt, you update the rule. I wanted to build something that detects novel attack patterns by learning what normal looks like, then flagging deviations. That requires marrying real-time stream processing with ML inference at the edge."
  },

  "architecture": {
    "diagram_path": null,
    "diagram_alt": "SentinelAPI architecture diagram",
    "flow_summary": "Client → CloudFront → API Gateway → Lambda authorizer → Kinesis stream → ML inference (SageMaker) → WAF rule updates → DynamoDB threat log"
  },

  "highlights": {
    "items": [
      "Lambda authorizer with inline anomaly scoring — every API request is scored against a lightweight model loaded from S3, adding under 15ms of latency while blocking high-confidence threats before they reach the origin",
      "Kinesis Data Streams for real-time traffic ingestion — request metadata (IP, endpoint, latency, status) is streamed to Kinesis at ingest time, decoupling collection from analysis",
      "SageMaker endpoint for ML inference — an Isolation Forest model trained on historical traffic identifies statistical outliers; the endpoint is called asynchronously from a Kinesis consumer Lambda",
      "Automated WAF rule management — confirmed threat IPs are automatically added to a WAF IP set via Lambda, blocking them at the CloudFront edge with no manual intervention",
      "SNS alerting with severity tiering — low-confidence anomalies are logged to DynamoDB for review; high-confidence threats trigger immediate SNS alerts to an ops topic"
    ]
  },

  "metrics": {
    "items": [
      { "value": "", "label": "p99 authorizer latency" },
      { "value": "", "label": "False positive rate" },
      { "value": "$0", "label": "Idle cost" },
      { "value": "", "label": "Threat response time" }
    ]
  },

  "stack": {
    "aws": ["Lambda", "API Gateway", "CloudFront", "WAF", "Kinesis", "SageMaker", "DynamoDB", "SNS", "S3", "CDK"],
    "languages": ["Python", "TypeScript"],
    "frameworks_tools": ["scikit-learn", "AWS SDK", "GitHub Actions"],
    "concepts_patterns": ["Anomaly detection", "Real-time streaming", "Edge security", "ML inference", "Event-driven", "IaC"]
  },

  "challenges": {
    "items": [
      "Cold start latency for the Lambda authorizer — loading an ML model from S3 on every cold start added 800ms+ of latency. Solved by packaging the model into a Lambda layer and keeping the function warm with provisioned concurrency for the critical path",
      "Feature engineering at stream time — converting raw request logs into model-ready features (rolling averages, geo-distance deltas, endpoint entropy) required stateful aggregation inside the Kinesis consumer, which complicated exactly-once processing guarantees",
      "WAF rule update rate limits — AWS WAF imposes update frequency limits on IP sets. Batching threat IPs and applying updates on a fixed schedule (rather than per-event) was necessary to avoid throttling during attack bursts"
    ]
  },

  "demo": {
    "screenshot_paths": null,
    "video_demo_url": null,
    "readme_url": null
  }
}
